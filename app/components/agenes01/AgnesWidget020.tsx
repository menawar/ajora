import React from 'react';

export interface AgnesWidget020Props {
  id?: string;
}

export function AgnesWidget020({ id = "AgnesWidget020" }: AgnesWidget020Props) {
  return <div id={id}>Base Component AgnesWidget020</div>;
}
