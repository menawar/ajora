{
  "name": "ajora-push",
  "private": true,
  "version": "0.1.0",
  "description": "Ajora push notification service — draw results + streak nudges (#16)",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "tick:draw": "node src/cli.ts draw",
    "tick:streak": "node src/cli.ts streak",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "vapid": "web-push generate-vapid-keys"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.5.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/web-push": "^3.6.3",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=23.6"
  }
}
